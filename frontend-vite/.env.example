# Frontend Vite Environment Variables

# API Base URL
# Development (.env.local): http://localhost:5000/api
# Production (.env.production): /api (relative path - uses same origin as frontend)
# 
# Using /api in production means:
# - If frontend is at https://app.vercel.app → API calls go to https://app.vercel.app/api
# - Your backend must serve the frontend build AND handle /api routes
# 
# Alternative: Use full URL
# Production: https://user-management-api.onrender.com/api
VITE_API_URL=http://localhost:5000/api
